import React from "react";

export const CardComponent = () => {
  // header component for "People"

  const headerTitle = "People";
  return (<div className="card">
    <h4>{headerTitle}</h4>
    </div>);
};
